{% load_yaml as os_map %}

  default:
    pkgs:
      - curl
      - git
      - tmux
    grains:
      os_home: /home
      os_root: /

{% endload %}

{% set common = salt.grains.filter_by(os_map, default='default') %}
